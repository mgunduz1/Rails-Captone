
<h4 class="text-center fw-bold mt-3">All Groups</h4>


<ul class="list-group">
    <% @groups.each do |group| %>
    <%= link_to group_path(group), class:"text-decoration-none" do %>
    <li class="list-group-item mb-2 px-2">
    <div class='row gx-0 py-1'>
    <div class='col-3 p-0 my-auto'>
    <%= icon('far', group.avatar, class: 'fa-5x i-color' ) %>
    </div>
    <div class='col-6 d-flex flex-column justify-content-around p-0'>
    <p class="fs-3"><%= group.name.truncate(15) %></p>
    <small class="text-muted"><%= group.created_at.strftime("%a, %e %b %Y") %></small>
    </div>
    <div class='col-3 p-0 d-flex flex-column justify-content-between'>
    <p class="text-center i-color fs-6 fw-lighter mb-1"><span class="fs-4 fw-bold"><%= group.transactions.count %></span><br />Transactions</p>
        <p class="text-center price-color fs-6 fw-lighter mb-0"><span class="fs-4 fw-bold">$<%= group.transactions.sum(:amount) %></span><br />Total</p>
    </div>
        </div>
    </li>
    <% end %>

    <% end %>
</ul>

<div style="height: 15vh;">
</div>

<%= link_to new_group_path, class:"text-decoration-none" do %>
<footer class="footer fixed-bottom create-btn py-3">
      <div class="container d-flex align-items-center justify-content-center">
        <p class="text-white fw-bolder fs-2"> Create New Group </p>
      </div>
</footer>
<% end %>


